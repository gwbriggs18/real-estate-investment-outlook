services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # Set your key here or in a .env file (do not commit .env with real keys)
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY}
    env_file:
      - .env
